# Gradle
.gradle/
build/

# Android Studio / IntelliJ
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# Local config
local.properties
keystore.properties
*.keystore
*.jks

# Build artifacts
*.apk
*.aab
*.ap_
*.dex

# Trained ML model artifacts (committed selectively when ready, not by default)
*.tflite
*.onnx
*.pt
*.pth

# Cervical classifier IS committed (small, needed for app to run)
!app/src/main/assets/via_model.tflite

# Kotlin
.kotlin/

# macOS
.DS_Store

# Environment / secrets
.env
.env.local

# Local notes (not part of submission)
memory/

# OpenCV SDK (manual install, do not commit)
opencv-android-sdk/

# Gemma 4 LiteRT-LM model file (large; download separately, see README)
app/src/main/assets/gemma4.litertlm
app/src/main/assets/*.litertlm
app/src/main/assets/*.task
